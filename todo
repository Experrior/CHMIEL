Dominika:


    backlog functionality(
        editing sprints
        deleting sprints (only wihtout tasks and not started)
        -[wip]-change model for creating tasks,
    )
    logic for looking for users, board  [STRECH GOAL]






Mateusz:


    contiune work on charts(
    finish second chart,
    implement backend logic to provide data
    )

    hotfix:
        don't show user creds in api repsonses
        show whole user data in api reposne for task comments


Zuzia:

    issues page(
        edit tasks,
        adding comments
    )




backlog:
    add global cors to all endpoints, not to each method!
    handle exceptions in more graceful way for api
